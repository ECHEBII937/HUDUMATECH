<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container-fluid">
        <!-- Update logo to HUDUMATECH logo -->
        <a class="navbar-brand" href="/"><img src="hudumatech_logo.png" alt="HUDUMATECH Logo" width="40" height="auto" /></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <!-- Update home link -->
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                <!-- Update services link -->
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#services">Services</a>
                </li>
                <!-- Update about link -->
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#about">About</a>
                </li>
                <% if (token) { %>
                    <!-- Update 'Pesanan Saya' link -->
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/pesananSaya/Belum_Dibayar">My Orders</a>
                    </li>
                    <% if (token.role=='admin' ) { %>
                        <!-- Admin dashboard link -->
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/dashboardAdmin/customer">Admin Dashboard</a>
                        </li>
                    <% } %>
                <% } %>
            </ul>
            <form class="d-flex">
                <% if (token == null) { %>
                    <!-- Sign in button -->
                    <button class="btn active btn-outline-primary signin-button" id="signin-button" type="submit"><a
                            class="signinbutton" href="/signin">Sign In</a></button>
                <% } else { %>
                    <!-- Log out button -->
                    <button class="btn active btn-outline-primary signin-button" id="signin-button" type="submit"><a
                            class="signinbutton" href="/logout">Log Out</a></button>
                <% } %>
            </form>
        </div>
    </div>
</nav>
